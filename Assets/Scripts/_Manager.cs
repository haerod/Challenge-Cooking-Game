using Unity.VisualScripting;
using UnityEngine;

public class _Manager : MonoBehaviour
{
    public string hotDogDish;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
